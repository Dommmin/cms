apiVersion: v1
kind: Service
metadata:
  name: app-gotenberg
  namespace: app
  labels:
    app: app-gotenberg
spec:
  selector:
    app: app-gotenberg
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP

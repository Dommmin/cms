apiVersion: v1
kind: Service
metadata:
  name: app-client
  namespace: app
  labels:
    app: app-client
spec:
  selector:
    app: app-client
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: cms-redis
  namespace: cms-prod
  labels:
    app: cms-redis
spec:
  selector:
    app: cms-redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-redis-data
  namespace: app
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi

apiVersion: v1
kind: Service
metadata:
  name: cms-server
  namespace: cms-prod
  labels:
    app: cms-server
spec:
  selector:
    app: cms-server
    component: web
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP

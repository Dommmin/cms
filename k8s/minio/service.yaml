apiVersion: v1
kind: Service
metadata:
  name: app-minio
  namespace: app
spec:
  selector:
    app: app-minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
